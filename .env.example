# Backend (ingestion · analysis · API). Copy to .env or export in your shell.
# PostgreSQL DSN. Defaults to this value when unset (matches docker-compose).
DATABASE_URL=postgresql://civiclens:civiclens@localhost:5432/civiclens
