# Frontend dev server. Copy to .env.local.
# Base URL of the CivicLens API. Defaults to http://localhost:8099 when unset.
# (In the Docker image this is baked to /api and proxied by nginx instead.)
VITE_API_URL=http://localhost:8099
