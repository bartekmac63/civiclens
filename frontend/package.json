{
  "name": "civiclens-frontend",
  "version": "0.0.0",
  "private": true,
  "description": "React frontend for CivicLens (placeholder — not yet implemented)",
  "scripts": {
    "dev": "echo \"TODO: set up the frontend toolchain (Vite + React + TypeScript)\" && exit 1"
  }
}
